import { createContext } from "react";

// Context to store and manage global state throughout the app


// Create the DataContext that holds the app's shared state
const DataContext = createContext();

export default DataContext;